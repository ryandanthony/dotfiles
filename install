#!/usr/bin/env bash

set -e
git submodule sync --quiet --recursive
git submodule update --init --recursive 
./dotbot/bin/dotbot --plugin-dir dotbot_plugin_aptget --plugin-dir dotbot-vscode -p my-plugins/snap.py -d "${BASEDIR}" -c install.conf.yaml "${@}"